<%= form_with(model: categary, local: true) do |form| %>
  <% if categary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categary.errors.count, "error") %> prohibited this categary from being saved:</h2>

      <ul>
        <% categary.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
