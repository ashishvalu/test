<%= form_with(model: order_detali, local: true) do |form| %>
  <% if order_detali.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_detali.errors.count, "error") %> prohibited this order_detali from being saved:</h2>

      <ul>
        <% order_detali.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :order_id %>
    <%= form.text_field :order_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
