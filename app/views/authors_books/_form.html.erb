<%= form_with(model: authors_book, local: true) do |form| %>
  <% if authors_book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(authors_book.errors.count, "error") %> prohibited this authors_book from being saved:</h2>

      <ul>
        <% authors_book.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :author_id %>
    <%= form.text_field :author_id %>
  </div>

  <div class="field">
    <%= form.label :book_id %>
    <%= form.text_field :book_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
